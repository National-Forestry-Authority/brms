(function (Drupal) {
    farmOS.map.behaviors.farm_nfa_geolayers_map_activities = {
      attach: function (instance) {
        console.log('farmOS.map.behaviors.farm_nfa_map_activities.attach')
        console.log("hello")
       
      }
    }
  }(Drupal))